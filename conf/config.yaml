defaults:
  - clips: final_dataset
  - segment: sasvi_maskrcnn
  - preprocess: sasvi_masks
  - extract_geometry: default
  - track_objects: sasvi_masks
  - graph_extraction: sasvi_masks
  - eval: default
  - compute_metrics: default
  - _self_
  
# cholecseg8k_root: video01_00000
preprocessed_root: /home/data/tumai/splatgraph/data/preprocessed/clean/test_output/
output_root: output/final

seed: 1234

config_dumps:
  # - ${output_root}/config.yaml

hydra:
  job:
    chdir: false