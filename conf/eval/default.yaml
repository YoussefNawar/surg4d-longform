defaults:
  - triplets: default
  # - temporal: default
  # - spatial: default
  - _self_

# nulling skips
temporal: null
spatial: null

# Required output directory for benchmark results
output_dir: ${output_root}/benchmark

# Optional output override for benchmark results
output: null